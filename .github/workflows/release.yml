name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: netresearch/skill-repo-skill/.github/workflows/release.yml@075ab60ba90c7c057b96f5a5b32dc48a5085d402 # main
    permissions:
      contents: write
