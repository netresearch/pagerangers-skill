name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions: {}

jobs:
  ci:
    uses: netresearch/skill-repo-skill/.github/workflows/ci-python.yml@075ab60ba90c7c057b96f5a5b32dc48a5085d402 # main
    permissions:
      contents: read
    with:
      python-versions: '["3.12", "3.13"]'
      test-command: |
        uv python install "$PYTHON_VERSION"
        uv sync --extra dev
        uv run pytest --cov=scripts --cov-report=xml --cov-report=term-missing
      upload-coverage: true
      coverage-files: 'coverage.xml'
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
