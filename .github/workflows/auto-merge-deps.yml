name: Auto-merge dependency PRs

on:
  pull_request:

permissions: {}

jobs:
  auto-merge:
    uses: netresearch/skill-repo-skill/.github/workflows/auto-merge-deps.yml@30bf8a094c7fda2e1855cec9c149235bb6fdbfca # main
    permissions:
      contents: write
      pull-requests: write
