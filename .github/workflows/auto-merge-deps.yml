name: Auto-merge dependency PRs

on:
  pull_request:

permissions: {}

jobs:
  auto-merge:
    uses: netresearch/skill-repo-skill/.github/workflows/auto-merge-deps.yml@075ab60ba90c7c057b96f5a5b32dc48a5085d402 # main
    permissions:
      contents: write
      pull-requests: write
